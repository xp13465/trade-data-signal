# 会话收尾总结 · v1.1.2 凯利三键改造上线(2026-08-18)

> 主控收尾总结,供用户醒来验收后 clear 会话。覆盖:v1.1.2 全链路上线、reviewer 双轮验收、优化批次状态、A 小册状态、未决事项。
> 上下文:2026-08-17 深夜跨 08-18 凌晨运行,用户去睡,授权"弄完收尾总结好"。

## 一、v1.1.2 凯利三键改造 —— 上线完成,§8 三查全 PASS ✅

### 改了什么(用户 2026-08-17 拍板)
- **excludeSpecialBear 主键(默认开)**:判定从「MA60 熊」升级为**四档大盘状态**(读 index_daily hs300):{熊市·主跌, 下降期}×buy_special×A股类。动到 AI 推荐/降亏过滤核心默认语义 → 按 §5.4⑥ 发 **v1.1.2 中间版本**。
- **legacyMa60Special(默认关备选)**:保留 v1.1.0 老 MA60 熊判定 × 追买。
- **declinePhaseSpecial(默认关备选)**:下降期 × buy_special,全市场。
- 三键全带 🆕NEW 标签;8 键+1 类数量不变(2 备选默认关不计数)。

### 回测复现
- R1_all ΔR1 = **+65,551**(基线 +910,465 → +976,016),逐位一致。
- 四口径对比报告:`docs/market-state/kelly-4tier-lagexempt-compare.md`。

### 首页/凯利/邮件三处一致(reviewer FAIL-1 返修)
- `_AI_MACRO_FILTER_NAMES` 缩 8 键 + 新增 `_AI_MACRO_BACKUP_NAMES`(备选仅 tooltip 展示不进判定);check_signals.py `AI_MACRO_KEYS` 同理。备选键默认关下首页永不因备选键判 AI降亏。

### 历史四档轨迹图展示位(阶段二)
- 走势图四档色带 + 独立时间线面板(`market_tier_history.json` 5773 条,2002-11-08 起),默认近 1 年可切 2002。
- reviewer FAIL-2 返修:fetchJSON 漏 `_R2_DATA_BASE` 前缀 → 修复 URL 后上线。

### 版本与部署
- merge main @ **4766bfe0c**;git tag **v1.1.2 @ 4766bfe0c**(force 移到最终 HEAD;版本链 v1.0.0/v1.1.0/v1.1.1/v1.1.2 四连齐)。
- 版本串 **a335**(sw.js CACHE_VERSION v6-20260818-a335 同 commit;补 about/privacy bump 4f07a0a2d)。
- deploy 两次:第一次 §24⑤ 校验 FAIL(common.min.js mtime < common.js,根因=deploy git pull 刷新源 mtime,内容 md5 一致=纯误报)→ touch 后重跑 exit 0。**落档 memory deploy-pull-main-touches-source-mtime**。

### §8 三查验收(全部通过)
| 查 | 证据 |
|---|---|
| ① main 链 | 4766bfe0c 已 push origin/main |
| ② 数据层 | summary.json `market_state`(当前=上升期,close 4741.1)+ R2 `market_tier_history.json` HTTP 200(344KB) |
| ③ 前端展示层 | 线上 app.min.js 含 market_tier_history/_AI_MACRO_BACKUP_NAMES/_isAiFadeHit/三键;lab.min.js 含三键 tooltip(老MA60/下降期) |
| 版本串 | 线上 index.html 307→/ 跟随后 **v=20260818-a335** ✓(之前 grep 空=未跟随 307,非缓存问题) |

### 落档
- memory `test-baseline-v112-anchor.md`(implementer 拍板定义 + 主控上线确认段)
- CLAUDE.md §5.4① 基准定义更新(四档升级仍 8 键+2 备选默认关不计数)
- README §23.1 补 v1.1.2 条;docs/pending-features-index.md「十一、v1.1.2 与优化批次增补」+ 索引 commit 0808a20ef

## 二、reviewer 双轮验收(§15 B 级改动)

1. **第一轮 FAIL(2 项)**:①§23.7 行为扩大(备选键进判定)+ §22 口径不一致(_aiOnMembers 遍历 10 键 vs tooltip 写 8 键)→ 返修 8 键白名单拆分 ②时间线面板 fetchJSON URL 漏前缀 → 返修。
2. **返修后第二轮 PASS**:8 键白名单 node 仿真(_isAiFadeHit=false)+ URL 修复 + 默认近 1 年。

## 三、优化批次状态(optimization-closeout-list.md 44 项治理)

| 批次 | 内容 | 状态 |
|---|---|---|
| **A** | O1 deploy 4遍→1遍 + ab39 增量导出 | ✅ 已完成(commit 657607b3d,任务 #12) |
| **B** | ab37 降并发+熔断 + ab38 core 采集提速 | 🚀 已派 implementer(worktree) |
| **C** | O2 etf_score 提速(workers 6→8-10) | 🚀 并入批次 B 同一 implementer |
| **D** | #51 宇宙规则首页 1:1 遵从走查 + #56 signal_notified 双副本清理 | 🚀 已派 implementer(worktree) |
| **E** | #46 降亏面板口径标注+真实对照行 | 🚀 已派 implementer(worktree) |

> 各批次改动文件不重叠(B 采集层 / D 前端+数据 / E lab.js 展示),并行 worktree 隔离。完整 update_all 回归待明天盘后。

## 四、A 小册 UUMit 发布(188 UT)

- 资产 265467f2 内容审核**持续 pending**(审核信号三通过但门禁字段不翻,平台卡滞非我方问题)。
- cron 1ff58872 **每小时**检查(用户要求 8min→30min→1h,晚上没人审),翻 pending 即自动发布+更新文档。

## 五、未决事项

1. **优化批次 B 的 ab38 删 sw 指数**:波及 board_etf_map/凯利回测/首页(§22/§23.6),implementer 已钉死"删前 grep 全站引用+列波及面,未确认的删项暂停回报",如需用户拍板会挂起。
2. **A 小册发布**:等平台审核,用户醒来后可手动查或等 cron。
3. **批次 D/E 结果**:implementer 完成后主控验收。
4. 用户醒来:看本总结 → 验收 OK → clear 会话。

## 复现

- 本总结来源:v1.1.2 上线链(memory test-baseline-v112-anchor.md 上线确认段)+ optimization-closeout-list.md 批次清单 + deploy 日志(/tmp/deploy-v112.log, /tmp/deploy-v112-2.log)。
- 验证命令:
  - `git log origin/main --oneline | grep 4766bfe0c`(merge 在 main)
  - `git tag -l`(v1.0.0/v1.1.0/v1.1.1/v1.1.2 四连齐)
  - `curl -sL "https://ss.fx8.store/index.html" | grep "v=20260818-a335"`(版本串在线)
  - `curl -s "https://ss.fx8.store/r2/data/market_tier_history.json" -o /dev/null -w "%{http_code}"`(轨迹数据)
- 数据截止:2026-08-18 01:00。
