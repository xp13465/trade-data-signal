# v1.1.10 标签后全量回归 Review(上线前把关)

> 审查日期:2026-08-30 深夜;审查人:reviewer agent(独立只读,不改代码不推 main)
> 审查范围:`git log v1.1.10..HEAD`,53 commits(含 lab-min 列修复 179354460 + 每日 token 统计 chore),改动文件 35 个,+7144 行
> 当前 HEAD:86dedbfa2
> 进度文件:`/tmp/agent-progress-v1110-review.md`

## 0. 结论

**PASS** —— 无 P0/P1 阻断项。共享核单实现、真实权威数逐位一致、表头/body 全表对齐、公示同步、无回归、无静默失败。2 个 P2 观察(见 §7)不阻塞上线,建议下一迭代顺手收口。

---

## 1. 维度1:§22 数据一致性 + 共享核正确引用 —— **OK**

**核心:common.js 挂共享核,lab.js/app.js 单实现防漂移。**

- **共享核 single source of truth**:common.js L1145-1237 定义 `_gihRealNavEnsure`/`_gihRealizeRealForce`,挂 `window._kkellyRealNavEnsure`/`window._kkellyRealizeRealForce`。
- **lab.js 优先调共享核**:`_kellyRealNavEnsure`(L7894,带 `!== _kellyRealNavEnsure` 自守卫防递归)与 `_kellyAihlineRealizeReal`(L7914)均优先走 `window._kkelly*`,生产恒走单实现;lab 自带 fallback 仅 common 不可用时兜底。
- **口径逐位一致**:common `_gihRealizeRealForce` 无 feeCfg 时默认 FEE_MAIN(买 0.00005/卖 0.001/min0.1/沪过户 0.00001/印 0 + ORIG_SLIPPAGE 0.001 还原 close),与 lab fallback 的硬编码 FEE_MAIN 完全一致;`_gihDaySpan`==`_kellyAihlineDaySpan`、`_gihIsShEtf`(51/58)==`_kellyIsShEtf` 逐位相同。**共享核返回对象无 `closed` 字段,lab fallback 有——但 `closed` 是 FIFO cap 内部 trade 对象字段,不来自 RealizeReal 返回值,消费方只读 pr/rp/hd/sell_price/flag,兼容。**
- **首页 app.js 复用同核**:`_simGhiHoldCap`(L3702,强平日无 feeCfg→FEE_MAIN 写回 selRow,标 `_gihForced`/`_gihNavMissing`)→ 渲染 `_simBtCalcRowRealForce`(L4411/4464)再套用户费率 `fp` 重算显示。
- **首页与 lab 双展示位一致性**:
  - 同一费率档下:首页 `_kkellyRealizeRealForce`(feeCfg=fp)与 lab `_kkellyRealizeRealForce`(feeCfg 无,=FEE_MAIN)同实现;fp=etf_main 时逐位一致。
  - 缺价(nav_missing):首页标 `_gihNavMissing` 红字「—缺价」跳过统计;lab 同样 `profit===null` 剔除统计+红字横幅,计数同挂 `window.__gih_missing_px_` 单点。**两展示位缺价口径一致。**
- **G/H/I 真实权威数三展示位一致**(卡片表/对比表/tooltip/advice/purpose-notes):G 157.74%/+157,743/年化6.26%/强平420/负年2011,2013,2021,2023;H 230.31%/+115,157/年化7.96%/无强平/负年2011,2012,2013,2023;I 156.21%/+140,585/年化6.22%/强平392/负年2011,2013,2021,2023 —— **全部与 `docs/kelly/position/scripts/kelly_ghi_real_price_out.json` real 字段逐位一致**(G real 157.7426/157742.64/6.2582/forcedN420;H 230.3144/115157.21/7.9617/forcedN0;I 156.2059/140585.33/6.2174/forcedN392;负年由 by_year 重算一致)。

## 2. 维度2:§15 回归 —— **OK**

**2a. 全信号表表头↔body 列对齐(源码+min)**
- 表头(L11580)14 列:`模式/半凯利仓位/胜率/盈亏比/单笔平均收益率/样本/最终盈亏(元)/峰值资金收益率/费率消耗/最大持仓/持仓中/年化/最大回撤/夏普卡尔玛`。
- body(L11540-11545)14 td 逐列对应,最大回撤列(第13)已在年化(第12)后,夏普卡尔玛合并列最后。**源码对齐。**
- lab.min.js:`<th>年化</th><th>最大回撤</th><th>夏普<br>卡尔玛</th>` + body 顺序 `mc→holding→I(年化)→P(最大回撤)→sharpe-calmar`,**min 与源码一致**(179354460 重建 + bump a516)。

**2b. §23.2 排查同类(其他表表头↔body)**
- 交易记录弹窗:colDefs 13 列 vs `_rowHtml` 13 td 逐列对齐(代码+名称合列、份额+每笔金额合列均 header-body 一致);被淘汰区 `colspan="13"` 与 13 列一致。**OK。**
- GHI 三档对比表:表头「模式|G|H|I」+ body 场景行「真实权威数」3 指标行一一对应 3 模式同列。**OK。**

**2c. ETF 走势 pin/缩放/配对(最大新功能)**
- panZoom 共享核(app.js `_etfTrendLiteBind` 新增 `opts.panZoom`,缺省 false):非 panZoom 分支 = 旧行为逐位不变(回归锚);panZoom 分支初始全窗口 [0,n-1]=scale=1=旧行为;mousemove/mouseleave 共享 `_vgeom` 两分支都 set;返回 `_ctl` 其他调用点忽略。**零回归。**
- 作用域 bug(变量块内声明块外引用致 ReferenceError 静默)已修:变量提到块外、`_n/_minWin` 改名 `_panN/_panMinWin`;`svg._etfTrendPan` 在首次 `_kernelRedraw()` 前赋值(L23842<L23846),无 undefined。**OK。**
- pin 配对:改用**原交易行引用 t**(buy.t===sell.t)而非"日期FIFO+下标"配对,消除交错时张冠李戴(P1-2 修正)。**正确。**
- popover 固定顶部 info 条不遮挡 pin(需求2);缩放/平移后 `etf-trend-panzoom` 事件驱动 `_placePins` 重排;切区(正式/淘汰)统一区间基准;返回后高亮定位触发行+淡出。**结构完整。**
- `_etfTrendLiteHTML/_etfTrendLiteBind/_etfTrendGeom` 为 app.js 顶层 function(全局),lab tab 可直接复用,无 scope 风险。**OK。**

**2d. 全信号表 grid 布局**
- `.lab-sigkelly-all-main`: `repeat(auto-fit, minmax(min(100%, 700px), 1fr))` —— 宽屏(容器≥2×700+gap)2 列并排,窄屏(<700px)min(100%)回退 1 列(与象限卡同法,无显式断点)。**正确,窄/宽屏均覆盖。**
- 夏普卡尔玛合并列 + 单笔平均收益率两行精简 + 最大回撤移位均只动列序/文案,不影响数据。

## 3. 维度3:§21 公示同步 —— **OK**

- purpose-notes.js `lab.sigkelly` 段:已含「废弃 b0/b1 估算(2026-08-30)强平日按主库 etf_daily.accum_nav 真实净值价重算」+「H 主推两条硬道理」+「⚠P3d 红色警示(稳定性存疑+实操性差)」+「G 157.74% vs H 230.31% 对比」,与 lab.js/data 逐位一致。
- app.js 首页 sim:`sim-gih-note` 文案「成交价按真实净值与真实交易日期记录印证, 收益率随费率档可调」+ line3766「与卡面 lab 权威数允许差异…非 bug」+ L48524「模拟值·收益随费率档变化,精确回测以卡面为准」。**算法/口径改动公示到位。**
- 主页 GHI 红字警示 class(`.sig-kbtn-help-pop-warn-red`)已随 style.css 补 CSS。**OK。**

## 4. 维度4:口径正确性 —— **OK**

- **真实权威数口径**:强平日按 accum_nav_map 真实净值重算,FEE_MAIN 费,与 lab 卡面/对比表/首页一致(见维度1)。nav_missing 硬报错,禁 b0/b1 兜底(用户铁律)。
- **b0/b1 彻底废除**:lab.js `_kellyAihlineApply` 仍算 b0/b1 仅作历史对照(注释明示「b0/b1仅历史对照不再作权威」);权威口径走 `real`;`__gihreal`=权威 stats(剔除 nav_missing 行);`__gihb1` 直接= `__gihreal`(旧键兼容等价 real,无 b1 fallback,注释明示);卡片/对比表/TOP1 读 `__gihb1`=real。**显示权威值全来自 real。** `__gihb0`(保守 0 利估算)仅 GIH off 时作参考(非权威)。**无 b0/b1 估算作为权威值残留。**
- **首页与 lab 逐位一致条件**:首页强平行渲染用 `_kkellyRealizeRealForce`(feeCfg=fp),lab 用同核(feeCfg 无)。fp=etf_main 时逐位一致(见 §7 P2-1 关于默认档)。

## 5. 维度5:代码健康 —— **OK**

- `_gih*` 函数 common.js 唯一定义,无重复/无冲突;`window._kkelly*` 全局名唯一。
- lab `_kellyRealNavEnsure` 有 `!== _kellyRealNavEnsure` 自守卫,防自递归。
- 无新增 debug residue(仅既有 notify/sigkelly 加载 console.log,非本 range 引入)。
- pin 配对用原行引用,非下标,防错配。

## 6. bj50/宇宙规则修复(3582cc95d) —— **OK**

- `build_board_etf_map.py`:从 `TRACK_INDEX_KW` 移除 bj50 + 新增 `_HOLDINGS_EXCLUDE={bj50,bj_399,csi_930820,ftse100,kospi}`,防 holdings/sum_pct 第4层误填 empty_array 类别导致 §23.6 对称校验 FAIL。
- 与 `universe_rules.yaml` empty_array 类别(bj50/ftse100/kospi/csi_930820 等)一致;deploy 链 check_universe_alignment.py FAIL 兜底。见 §7 P2-2。

## 7. P2 观察(不阻塞上线,建议下一迭代收口)

- **P2-1(app.js L4666 注释不准确)**:`_simBtCalcRowRealForce` 注释称「默认档 etf_main(万0.5/最低0.1/免印花)与 FEE_MAIN 逐位一致」,但首页 sim 实际默认档是 `etf_def`(万3/最低5/印万5,`_simBtReadFp` L4586),**≠ FEE_MAIN**。即默认情况下首页 sim G/H/I 数字 ≠ lab 卡面权威数。此差异已在 sim-gih-note 文案声明(「与卡面允许差异…非 bug」),且 etf_def 默认是本 range 之前就有的(非本次引入),**不算新回归**。但注释与默认档不符会误导后续维护,且用户默认打开首页 sim 看到与 lab 卡不一致的 G/H/I 数字,建议:①注释改正;②或默认档对齐 FEE_MAIN(etf_main)以默认逐位一致(需用户拍板,涉及默认行为)。
- **P2-2(bj50 代码 exclude 与 yaml 不完全对齐)**:`_HOLDINGS_EXCLUDE` 含 `bj_399`,但 `universe_rules.yaml` empty_array 未列 `bj_399`;且 yaml 声明 ~30 个 empty_array 指数,代码只 exclude 5 个(其余靠"无匹配 ETF 自然留空")。当前由 deploy 时 check_universe_alignment.py 对称校验兜底,未触发 FAIL 即上线安全。建议核对 `bj_399` 是否应补登 yaml(数据域,非前端)。

## 8. 上线动作(供主控)

- merge 已含本 range 全部 commit(HEAD=86dedbfa2),`bash scripts/deploy.sh` 推前端 a516 + 数据后,验三站 lab 弹窗列对齐 + 首页 sim G/H/I 数字。
- 部署后 `git ls-files` 确认本报告 tracked。

## 9. 复现/核实源

- 共享核:common.js L1145-1237;lab 委托 L7892-7920;app 首页 L3642-3730/L4390-4470/L4663-4700
- 权威数据:`docs/kelly/position/scripts/kelly_ghi_real_price_out.json`(strats.G/H/I.real + by_year)
- 表头/body:lab.js L11530-11581;min `lab.min.js`(年化→最大回撤→夏普卡尔玛顺序)
- 宇宙规则:config/universe_rules.yaml L66-111 + build_board_etf_map.py `_HOLDINGS_EXCLUDE`
