# 固化快照修正 vs 回测口径前视偏差 —— 双口径 A/B 对比调研报告

- 调研日期:2026-09-23
- 调研主题:commit 991c27dfb(首页历史信号固化快照,根治 T+1 晚到数据漂移)只修了实操计划层(买入价/持仓 aligned 到次日真实价位),**未改回测脚本**;因此需要回答用户疑问——回测口径是否仍含「未来才知道」的晚到信息(前视),影响有多大,需不需要翻案主推结论
- 状态:定稿(reviewer 复核通过后可归档);**2026-09-24 对账修正**(§5.4⑦):主表/结论文档数字整体切页面默认档 etf_main,researcher 旧口径数字标注已废弃并留修复链(见 §8.2/§8.3)
- 数据版本:线上 signal_kelly_trades.json generated_at = **2026-09-23 22:28**(信号范围 20110119~20260922,与 A 口径复刻环境同版本)

## 1. 结论速览(直接回答用户疑问)

0. **Headline(页面默认档 etf_main·万0.5/最低0.1,对账修正后口径)**:A(现状·含晚到)= **554 笔 / +162,284 元 / rmh 162.28%**;B(剔除 70 条晚到)= **546 笔 / +163,646 元 / rmh 163.65%**;峰值持仓均为 10 笔。此对账修正闭环节点见 §8.2,researcher 旧口径数字(154,336 等)**不再是任何档位的 headline**,仅作已废弃口径保留于 §8.3/§9 可反查。
1. **回测口径 = 历史最终数据(含晚到),不是 bug,是口径与实操层的机制差**。`signal_kelly_backtest.py` 读 `signal_daily` 全表最终版生成回测信号集(不区分晚到),晚到信号 = 8-12~9-22 共 70 条;固化快照修的是实操计划层买入价/持仓到次日真实价位,不改变回测信号集。
2. **前视偏差方向 = 含晚到略微低估收益**(2026 年 8-12 之后的晚到信号净亏约 -1,362,口径 etf_main,即 B−A 总盈亏差值),方向与直觉相反(不是「回测偷看未来多赚钱」,而是「回测把晚到也当可买信号记了亏」)。researcher 旧口径净亏 -1,473 一并修正。
3. **修正幅度很小(etf_main 口径重跑)**:全信号表口径 Δrmh **+1.37pp**、Δ总盈亏 **+1,362 约 +0.84%**;16 象限各档 rmh 普遍 +0.1~+0.9pp(researcher etf_def 口径样本,方向一致,见 §4.2 标注)。方向、量级、笔数结构在口径切换后**不变**。
4. **无任何主推结论或测试基准需翻案**:2011-2025 历史段逐年逐位完全相同(已按 etf_main 复验),仅 2026 不同;基准定义、默认组合、公示文案**全部不需要动**。

## 2. 影响面

| 层 | 本次固化修正(991c27dfb) | 回测口径 |
|---|---|---|
| 实操计划层 | ✅ 改了 `scripts/nextday_plan_generator.py`(买入价/持仓 aligned 次日真实价位,冻结快照) | 不受影响 |
| 前端展示 | ✅ 改了 app.js(首页信号卡/建议按快照展示) | 前端全信号表仍实时重算(读回测产物 + 前端过滤) |
| 回测脚本 | ❌ 未触及 | `signal_kelly_backtest.py` 无快照/晚到相关逻辑(见 §3 代码证据) |
| 数据产物 | signal_kelly_day_snapshot.json 等新增快照产物 | signal_kelly_trades.json / signal_kelly_backtest.json 生成链路不变 |

影响面结论:本次修正不构成「回测有前视 bug」,不触发 §5.4⑥ 版本升级 / 基准重定 / 公示改动。

## 3. 回测信号集来源(代码证据)

`scripts/signal_kelly_backtest.py` L1779-1786:

```python
# 2. 读买信号
print("-> 读 signal_daily 买信号 ...", flush=True)
conn = get_conn()
buy_rows = conn.execute(
    f"SELECT date, index_id, signal FROM signal_daily "
    f"WHERE signal IN ({','.join('?' * len(BUY_SIGNALS))}) ORDER BY date",
    BUY_SIGNALS,
).fetchall()
```

- **直接读 `signal_daily` 全表最终版**,不区分晚到 → 回测口径 = 历史最终数据(含晚到)。
- 晚到判定(实操层,`scripts/nextday_plan_generator.py` L549-556,与首页 overview 同款三条件):非 global/hk 市场 ∧ 当日盘中无首现 ∧ 当日盘中轮覆盖完整(覆盖率≥3 且收录时点 ≤17:00)。
- **晚到判定依赖 `signal_intraday_log` 的盘中首现记录**,该表仅 20260810 起有覆盖率 → 8-10 之前的历史晚到**无法判定**,未纳入统计(见 §7 诚实标注)。
- 晚到信号 = 8-12~9-22 共 **70 条**(A 口径 signal_daily 全表 71,246 条 → B 口径剔除后 71,176 条;对应买信号 A=42,048 / B=41,978)。

## 4. 双口径 A/B 对比(前端全信号表口径)

口径说明:16 象限 A 模式去重(评级三区并集)+ S06 动态降亏(a9/new14 按日切)+ K=1 每日池 + **etf_main 费率(万0.5/最低0.1)= 页面默认档**(2026-09-24 对账修正,原 researcher 口径 etf_def 已废弃,见 §8.2/§8.3)。

| 指标 | A(现状:含晚到) | B(固化:剔除 70 条晚到) | Δ |
|---|---|---|---|
| 成交笔数 | 554 | 546 | -8(净;14 晚到剔除 / 6 笔被挤信号回流) |
| 总盈亏 | +162,284 | +163,646 | +1,362(约 +0.84%) |
| 累积盈亏%(rmh) | +162.28% | +163.65% | +1.37pp |
| 峰值持仓 | 10 笔 | 10 笔 | 0 |
| 最大回撤(已实现曲线) | -3,833(谷底 20260923·2026 尾部回撤) | -3,744(谷底 20190107) | 见下方标注 |

**最大回撤口径与复验说明(2026-09-24)**:已实现曲线=按卖出日累计已实现盈亏(未平仓按当前价标在期末)。etf_main 重跑后 **A 最深回撤移到 2026 尾部(谷底 20260923,-3,833),B 仍为 20190107(-3,744)**;原 researcher 报告两者均记 -3,951(谷底 20190107),其中 A 的 20190107 回撤在 etf_main 下为 -3,744(与 B 一致,2011-2025 逐位相同),但 2026 尾部回撤更深,故 A 谷底变更为 20260923。回撤绝对值随费率档下降而同幅缩小(费用减少→曲线更高),方向与量级不受影响。

> 对账锚点(etf_main 档):A 按脚本重跑 162,283.80 ≈ 页面 162,284(逐位,PASS,见 §8.2);B 脚本重跑 163,645.92,页面无 B 档直接展示,数值源=脚本 etf_main 实跑。

### 4.1 按年分解

2011~2025 逐年**逐位完全相同**(etf_main 口径复验,2011/2013-2025 逐年 A≡B);仅 2026 不同(A 69笔+19,191 / B 61笔+20,554,口径 etf_main;researcher 旧口径 18,200/19,674 已废弃)。2026 差异结构:
- A 多出的 8 笔 = 14 条晚到信号中 8 条在 K1 池内被计入(K1 池内晚到共 14 笔),其余 6 笔被「同池挤掉/回流」机制替代(结构判断与 researcher 一致,仅绝对数值换口径)。

### 4.2 16 象限全量差异表

> **口径标注(2026-09-24)**:本象限全量差异表(约 700 行)与下表代表样例均为 **researcher 口径(etf_def)** 生成,非 etf_main;etf_main 只重跑了三区并集层(A=+162,284 / B=+163,646,见 §4),**象限级细分未在原复刻脚本覆盖范围内**,未逐 quadrant 重跑。方向性结论(剔除晚到后各档 rmh 普遍略升)已由并集层 etf_main 重跑(+1.37pp 同向)佐证;若后续要把象限绝对数值用于其他结论,先按 §8.3 差额桥接或重跑象限级。

全量 16 象限 × 11 模式 × 5 周期差异表(约 700 行)见调研证据文件 + 复现脚本可重算(researcher 口径)。各象限 rmh 普遍 **+0.1~+0.9pp**,方向一致(剔除晚到后各档收益普遍略升,与总体结论同向)。代表样例(researcher 口径,只作文向参考):

| 象限 | 周期 | 档 | nA→nB | rmh A%→B% | Δrmh |
|---|---|---|---|---|---|
| rating_high | all | A | 86→85 | 17.60→17.95 | +0.35 |
| etf_strong | all | A | 1,525→1,497 | 12.43→13.32 | +0.89 |
| mkt_concept | all | A | — | 14.61→14.99 | +0.38 |

(完整表见 `/Users/linhuichen/.claude/projects/-Users-linhuichen-code-trade/3ba5f717-05ed-4419-abe0-6426872cd686/tool-results/bwt6gukbk.txt`,共 716 行,以「象限 周期 档 nA nB Δn rmh_A% rmh_B% Δrmh」列。)

## 5. 前视偏差量化结论

- **方向 = 含晚到略微低估收益**(etf_main 口径重跑):晚到信号 2026 年 8-12 后净亏约 **-1,362**(剔除后收益反而升高;researcher 旧口径 -1,473 已修正)。
- 固化快照修的是实操层(买入价/持仓 aligned 到次日真实价位),**不改变回测信号集** → 这不是「回测有前视」的 bug,而是回测口径与实操层存在约 1.4pp(rmh)差异。
- 晚到信号本质上 = 「信号日盘中才知道、回测却按最终数据当作当天已知信号」,量化下来对收益率的影响是**反向的(更保守/低估)**,不构成虚高收益。

## 6. 修正幅度结论

- 前端全信号表口径(etf_main 重跑):Δrmh **+1.37pp** / Δ总盈亏 **+0.84%**(researcher 旧口径 +1.47pp/+0.95% 已修正)。
- 16 象限各档 rmh +0.1~+0.9pp(researcher etf_def 口径样本,方向参考)。
- **无任何主推结论或基准定义(§5.4 memory `test-baseline-v112-anchor`)需翻案**——2011-2025 历史段零变化,2026 差异在量级内且方向偏保守。

## 7. 诚实标注(完整保留)

1. **锚点对账**:memory `kelly-backtest-caliber-authority` 记的锚点 `all A=161.63%` 是 **9-22 15:38 旧数据版本**实测(当时线上数据到 9-15);当前数据(到 9-22)+ 页面默认档 etf_main = **162.28%**(researcher 旧口径曾报 154.34%,已废弃,见 §8.3)。**161.63% → 162.28% = 数据版本推进 + 口径修正所致,非逻辑漂移**(详见 §8 复现段对账证据)。
2. **晚到判定数据依赖**:晚到判定依赖 `signal_intraday_log`(仅 20260810 起有覆盖率)→ 8-10 之前的晚到无法判定,未纳入统计,**存在未量化历史残留**(方向 = 剔除晚到收益更好,只会让「现在略低估」更成立)。
3. **近似的边界**:快照仅覆盖 30 日,B 口径用「剔除晚到」近似「固化当时可得」,**近似不精确**。
4. **K1 每日池口径的限制**:K=1 每日池口径下晚到影响 14 笔;非 K1 档(K=2/3/4)仅由 16 象限全量表覆盖(rmh +0.1~0.9pp),未单独按池重算。
5. **未平仓处理**:K1 池中未卖出持仓按 current_price 计算盈亏(与前端一致),T+1 之后仍可能随新数据小幅变动。
6. **绝对基准差异(重要·2026-09-24 更新)**:researcher 结论表与本次独立实测在**绝对基准**上有两级出入——①researcher 用的根本不存在于页面任一档(非标滑点 0.001254,见 §8.3,已废弃),②即使按其声称的 etf_def 档,页面/脚本实测均为 +157,233(+157,232.44)而非 +154,336;本报告主表已整体切到页面默认档 etf_main(+162,284)。researcher 的 A/B **方向、量级、笔数、按年结构**在口径切换后一致不变(§4 重跑确认),仅绝对数值与最大回撤谷底按新口径修正。绝对基准差异来源见 §8.3,不影响结构性结论。

## 8. ## 复现段(§5.4⑦ 同构对账 + 页面实测锚点)

### 8.1 复现工具
- 复刻脚本:`docs/kelly/analysis/scripts/signal_freeze_caliber_ab.py`(前端「全信号表」口径的 Python 同构实现:三区并集 → S06 动态降亏 → K1 每日池 → recompute 费率 → 峰值资金收益率)。
- A/B 回测数据环境:`/tmp/bk_env/{A,B}/out/signal_kelly_trades.json`(A=signal_daily 全量 71,246 条;B=剔除 70 条晚到后 71,176 条)。

### 8.2 三条线对齐(复刻脚本 → 页面实测 → 线上产物版本)
| 线 | A 档全信号表值 | 说明 |
|---|---|---|
| ① 线上产物版本 | generated_at=**2026-09-23 22:28**,信号范围 20110119~20260922,rating_high/A=86 条(CF+R2 双端点一致) | curl 线上 signal_kelly_trades.json 验证 |
| ② 复刻脚本(A 环境, etf_main 默认档) | **554 笔 / +162,283.80 / rmh 162.28% / 峰值 10 笔** | `signal_freeze_caliber_ab.py --trades-json /tmp/bk_env/A/out/signal_kelly_trades.json --label A`(默认 `--fee-preset etf_main` 与页面默认档一致) |
| ③ 页面实测(无痕浏览器零 localStorage,默认档) | **554 笔 / +162,284 元 / rmh 162.28% / 最大持仓 10 笔 10.0 万** | `scripts/playwright-accept/probe-kelly-freeze-all-card.mjs`(Playwright 新建 fresh context,清 cookies/无 localStorage) |

**逐位对账结论:②=③(162,283.80 ≈ 162,284,rmh 162.28% 一致,554 笔一致,峰值 10 笔一致)= PASS。** 复刻脚本 ↔ 页面真实渲染 ↔ 线上产物版本三条线对齐。

> **§8.2 自相矛盾修复(2026-09-24)**:原报告写「脚本默认 `--fee-preset etf_main`」,但脚本 argparse 实际默认 `etf_def`。**本修订选择改脚本默认值(etf_def → etf_main)而非改报告文案**,理由:①脚本是前端同构复刻,使命=无参跑出页面默认档数字,默认档必须与页面一致(etf_main);②§8.2 原文案本就是正确意图,是脚本实现偏了;③改默认后 `--fee-preset` 不传即复现页面 162,284(PASS),杂音最少。脚本默认档注释/FEE_PRESETS 注释同步更正。

### 8.3 差异标注(researcher 结论表 vs 独立实测)【已废弃口径·修复链】
- **结论不变(成立):researcher 的 154,335.86 复现不了。** 四个数据版本(A 环境 / 线上 / 旧版 / B 环境)× 全部 5 个费率档(zero/etf_def/etf_main/etf_cheap/stock_def)逐一扫描,**没有任何一个页面可选档产出 154,335.86**。其声称的「etf_def」档实际页面/脚本实测 = **+157,233 / +157,232.44**(逐位一致)。
- **归属线索(2026-09-24 补)**:researcher 数字仅在**非标滑点 slippage=0.001254**(页面任一档都不是,页面滑点为 0.1%=0.001)下可复现:**154,337.64 / rmh 154.34%**,其中 rmh 与报告 154.34% 精确重合 → 判定 researcher 实测时用了页面/脚本任何档都不是的参数(滑点或佣金组合),疑为复刻脚本细节参数(佣金/滑点/最低佣金)与前端预设脱节。
- **判定口径(页面实测锚点优先,§5.4⑦)**:**researcher 的 154,336(以及 B 档 155,809)= 已废弃口径,不用于任何 headline/主表数字**;主表整体采用页面默认档 etf_main 重跑值(A=+162,284 / B=+163,646,见 §4)。结构性结论(方向、量级、笔数、按年结构、Δrmh 增量)在口径切换后一致不变(§4 重跑确认)。
- 原修复链证据(可反查):①线上产物版本已核对一致(22:28,与 A 环境同);②页面 etf_def 档(万3/最低5)自渲染 = +157,233;③脚本 etf_def 重算 = +157,232.44,与页面逐位一致;④researcher 的 154,335.86 无法由「同一产物 + 同一费率档」重现。

### 8.4 验证命令(可重跑)
```bash
# 1) 复刻脚本 A/B 双口径(默认 etf_main 档 = 页面默认档,与 8.2 对齐;2026-09-24 起脚本默认即 etf_main)
python3 docs/kelly/analysis/scripts/signal_freeze_caliber_ab.py \
  --trades-json /tmp/bk_env/A/out/signal_kelly_trades.json --label A \
  --trades-json /tmp/bk_env/B/out/signal_kelly_trades.json --label B \
  2>run.log
# 1b) 页面 etf_def 档交叉核对(researcher 声称口径,交叉验证仅此用途)
python3 docs/kelly/analysis/scripts/signal_freeze_caliber_ab.py \
  --trades-json /tmp/bk_env/A/out/signal_kelly_trades.json --label A --fee-preset etf_def --json
# 2) 页面实测(无痕零 localStorage, 页面默认档=etf_main 读 A; 切 etf_def 交叉核对)
node scripts/playwright-accept/probe-kelly-freeze-all-card.mjs
# 3) 接口 A/B 回测产物生成(若 /tmp/bk_env 已清理, 用 run_backtest.sh 重建)
bash /tmp/bk_env/run_backtest.sh A && bash /tmp/bk_env/run_backtest.sh B
```

页面对账原始值(2026-09-23 23:40 实测 + 2026-09-24 etf_main 全量重跑,平台 ss.fx8.store,产物 22:28):
- 默认档(etf_main,万0.5/最低0.1):A=554 笔 / +162,284 元 / rmh 162.28% / 最大持仓 10 笔·10.0 万(页面实测,脚本同 = 162,283.80)= PASS
- **B 档(etf_main,脚本重跑)**:A=546 笔 / +163,645.92 元 / rmh 163.65% / 最大持仓 10 笔(页面无 B 直接展示,以脚本为数值源)
- etf_def 档(万3/最低5,交叉核对仅此用途):A=554 笔 / +157,233 元 / rmh 157.23%(页面实测 = 脚本 157,232.44 逐位一致)

## 9. 与既有基准/锚点的一致性结论

- §5.4 基准定义(memory `test-baseline-v112-anchor`):**维持不变**(基准策略组合/默认值未动,本次纯调研落档)。
- memory `kelly-backtest-caliber-authority` 锚点(前端全信号表 all A=161.63%):**161.63% 对应 9-15 旧数据,当前 9-22 新数据 = 162.28%(页面默认档 etf_main,本报告权威口径)**,差异由数据版本推进 + 口径修正引起;回归锚点口径本身(同产物、同参数)复现一致,无口径漂移。researcher 旧口径 154.34%(非标滑点)已废弃(§8.3 修复链),此处不再并列。
- 前端算法公示(purpose-notes.js / app.js / lab.js):本次**未改任何算法/数值**,无公示同步需求。后续若回测口径升级为剔除晚到(非默认组合),才需同步公示(见 §10 建议,本次不动)。

## 10. 建议(供主控/用户决策,本次含晚到口径维持现状)
1. **回测口径维持含晚到(现状)**:偏差方向=略低估收益,且主推结论零影响;改动回测口径会影响历史全部回测数字,收益小成本高。
2. 若用户希望实操/回测完全对齐,可后续把「剔除晚到」作为回测可选项(默认可选不默认),需重新跑全部基准并走 §5.4⑥ 版本流程(本次不做)。
3. 已归档调研结论供后续做「晚到剔除回测选项」时直接复用(A/B 环境与复刻脚本已固化)。