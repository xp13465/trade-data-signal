# 云上 3 条 active 告警修复记录(2026-09-18)

> 用户 2026-09-18 授权「迁云后问题直接修」,实施 agent 运维修复。告警源:`alert_state.json`(云上 `/home/ubuntu/code/trade-data/data/alert_state.json`)。

## 结论速览

| # | 告警 key | 根因 | 修复动作 | 当前状态 |
|---|---|---|---|---|
| 1 | `etf_national_team|exit!=0|1` | 云上 `trade-data-signal` 仓库 `TASKS.md` L14 残留关闭指针 `#98 CodeGraph ui 待官方发包;`,而 pending-features-index #98 状态列已=「已上线/见 #80」→ `check_task_state.py` tasks_refs FAIL → deploy.sh 阻断 → backfill 综合 exit=1 | 云上 `TASKS.md` 移除 #98 指针,commit `8835d8842` push main | 修复生效,等今晚 20:07 定时跑 exit=0 后 schedule_monitor 自动恢复 |
| 2 | `com.trade.etf-national-team|not_loaded` | 同 1:service `trade-etf-national-team.service` 因 backfill exit=1 进入 `failed` 状态,`systemctl is-active` 输出 failed → schedule_monitor 判未加载 | `sudo systemctl reset-failed trade-etf-national-team.service` → 回 `inactive`(oneshot 正常态) | **已自动 recovered**(schedule_monitor 下轮检测恢复) |
| 3 | `lab_auto|TimeoutError:|a129e216` | R2 PUT `trade_sim_data/trade_sim_csi_399996_stats.json` attempt 1 瞬时超时(`TimeoutError: The write operation timed out`),1s 后 retry 成功,update_lab 退出码汇总全=0(9-17 19:00 运行成功) | `alert_ack.py` 人工确认(24h 静默) | acknowledged,等 24h 静默窗口自然淡出 |

## 一、告警 1+2 根因链(证据)

- `systemctl status trade-etf-national-team.service`:Loaded(static)但 `Active: failed (Result: exit-code)`,ExecStart=`/bin/bash .../etf_national_team_backfill.sh`,9-17 21:57 启动 23:10 exit=1。
- backfill 日志 `etf_national_team_backfill_20260917_2157.log`:采集/accum-nav/export_etf_hist 全部成功(COLLECT_RC/ACCUM_RC/ETF_HIST_RC=0),deploy 段 `deploy.sh` 被 `check_task_state.py` 拦:
  - `✗ tasks_refs: TASKS 编号对账 FAIL(1 处): 幽灵编号/残留关闭指针 #98`(L14 当前会话状态段)
  - `⚠ 存在任务状态漂移, FAIL 阻断上线(挂 deploy 链)`,`✗ deploy 失败 (rc=1)`。
- 9-17 20:07 主槽那轮 deploy 实际成功(21:06-21:57 exit=0),失败的是 21:30 兜底槽延迟到 21:57 起跑那轮;00:07/00:22 补跑轮同样被 #98 拦。

## 二、修复动作

1. 云上 `/home/ubuntu/code/trade-data-signal/TASKS.md` L14 删除 `#98 CodeGraph ui 待官方发包;`(pending 状态已是「已上线/见 #80」,#98 属残留关闭指针,不该再作待办指针)。
   - commit:`8835d8842 fix(tasks): 移除 TASKS.md 残留关闭指针 #98(...)` push main。
   - 复验:`check_task_state.py` → `✓ tasks_refs: TASKS 编号对账 OK ... 无残留关闭指针`;汇总 `2 ok / 1 warn(→zombie_crons 非阻断)/ 0 fail`,RC=0。
2. `sudo systemctl reset-failed trade-etf-national-team.service` → `systemctl is-active` 返回 `inactive`(rc=3)→ schedule_monitor `launchctl_loaded` 判定已加载 → not_loaded 恢复(已 recovered 实测)。
3. `python3 scripts/alert_ack.py lab_auto\|TimeoutError:\|a129e216` → acknowledged 2026-09-18 10:58:18。

## 三、同类错误面清单(§23.2 修 bug 三铁律)

| service | 状态 | 是否产生 active 告警 | 根因 | 处置 |
|---|---|---|---|---|
| trade-etf-national-team | failed→已 reset | 是(本次 3 条中 2 条) | #98 残留指针拦 deploy | 已修(见上) |
| trade-daily-brief | failed(Result: timeout) | 否 | `TimeoutStartSec=600` 9-17 20:40-20:50 超时被杀;日志显示邮件/飞书通知已发出,通知后步骤(JSON/push)未确认完成 | 不属本次 3 条 active 告警;9-16 已按 `timeout-caliber-20260916.md` 定 600=快任务正确口径,昨晚超时疑似挂死,建议观察今晚 20:40,持续挂死另开单排查 |
| trade-public-fund-daily | failed(Result: timeout) | 否 | `TimeoutStartSec=600` 9-17 17:00-17:10 被杀;日志显示 392MB export 已完成但 check_data_integrity/deploy 未跑完 | 同上;9-16 已定 600 正确(非慢任务);建议观察今日 16:30,超时说明 export 在云上慢/挂死需另排 |

> 注:两个 timeout service 均无 active 告警(alert_state 仅 3 条 active 全为本次任务对象),按「修完整」列明待观察,不擅自改 600 口径(9-16 已拍板收口,避免回退已决策项)。

## 四、恢复路径(告警 1)

- `etf_national_team|exit!=0|1` 的恢复依赖 `schedule_stats.json` 中 etf last_exit=0。
- 今日 9-18 为交易日,20:07 `trade-etf-national-team.timer` 主槽自动跑,修复后 deploy 不再被 check_task_state 拦 → backfill exit=0 → `gen_schedule_stats.py` 更新 schedule_stats → schedule_monitor 15min 周期检测 exit=0 → 该 key 自动 recovered。
- ⚠️ 未盘中手动跑 backfill:§3 硬铁律「交易日盘中(09:30-15:30)不跑全量 export+deploy」,盘中发布当日未定型 ETF 数据属用户可见数据改动,需主控拍板;默认走今晚 20:07 自然验证。

## 五、附带发现(建议,非本次修复范围)

- `schedule_monitor.sh` 的 log 异常检测(`update_lab_launchd.log` 是 append 累积)对「attempt 1 失败但 retry 成功」的历史行持续 seen(dedup_key=行 md5),**瞬时超时告警不会自动恢复,只能人工 ack**。建议后续 micro-task:`update_lab.sh` 对含「attempt N 失败 ... 重试」且最终该任务 exit=0 的行豁免,或 schedule_monitor 扫描限定「最近一次运行新增段」。
- 云上 `trade-data-signal` 工作区有 `M docs/kelly/position/scripts/accum_nav_map.json`(9-17 deploy 失败遗留脏文件),不影响下次 deploy(deploy.sh 只 add static-site/data/+min JS),未擅自处理。
