# 优化补丁巡检清单(2026-08-15 起,未来 5 天)

> 用户定(2026-08-15):P0 优化 + #90 修复落地后,需**定期巡检几天**,评估改完是**正向发展还是负向发展**。严重问题及时止损回滚,小问题继续迭代优化。

## 巡检对象(4 项改动)

| 改动 | 落地内容 | 分支/commit |
|---|---|---|
| #90 采集异常修复 | 恢复 `runner.py` 的 `_now()` 定义,重跑 backfill 补 8/14 缺口,重新 export/deploy | 修复 implementer 回报 |
| P0-1 主控会话瘦身 | `~/.claude/settings.json` COMPACT 阈值 1048576 → 600000 | 用户级配置(不在 git) |
| P0-2 implementer 约定清单 | `.claude/skills/role-implementer/SKILL.md` 加「约定遵循检查清单」 | 优化 implementer 回报 |
| P0-3 reviewer 瘦身 | `.claude/skills/role-reviewer/SKILL.md` 加「reviewer 瘦身规范」 | 优化 implementer 回报 |
| P0-4 派单锚点 | 派单必带 `@关键文件:行号`,防重读大文件 | 规范,落地位置回报 |

## 巡检指标(6 项)+ 判定标准

### 指标 1:线上采集健康(最关键,每天必查)
- **查法**:`curl https://ss.fx8.store/data/overview.json` 看 `collect_health` 是否还报 `_now`
- **正向**:collect_health 清零或只剩非 `_now` 的独立项(qvix 超时待观察)
- **负向(严重)**:仍报 `_now` → #90 修复没生效/没上线,需立即查 deploy
- **负向(小)**:其他独立 error(如 qvix 超时)→ 记录,下交易日观察

### 指标 2:#90 数据补齐(每天查)
- **查法**:查 daily_metric 表 `us10y`(美债)、`a_fund_margin`(两融)8/14 值是否已写入
- **正向**:两个指标 8/14 值已补齐
- **负向(严重)**:仍缺 → backfill 没跑成或补采失败

### 指标 3:8/17 周一盘中写入(关键验证点,8/17 盘后必查)
- **查法**:查 intraday 日志/collect_log,盘中宽度指标 `a_width_*`(涨停/跌停/炸板率/涨跌家数)当天是否正常写入
- **正向**:盘中每 10 分钟写入正常,无 `_now` 报错
- **负向(严重)**:仍写失败 → #90 修复没覆盖盘中链路,需立即回滚排查
- **负向(小)**:偶发超时(非 `_now`)→ 记录观察

### 指标 4:主控会话瘦身效果(P0-1)
- **查法**:`ls -lh ~/.claude/projects/-Users-linhuichen-code-trade/*.jsonl | tail` 看主会话 jsonl 大小
- **正向**:compact 阈值 600K 生效,主会话不再膨胀到百 MB 级
- **负向(严重)**:仍冲到 100MB+ → 阈值没生效/没 compact,需复查 settings
- **负向(小)**:会话仍偏大但可接受 → 提醒主控养成大任务后 /clear

### 指标 5:执行质量(P0-2/3/4)
- **查法**:数近期 reviewer FAIL 次数(implementer 打回率)、reviewer 是否漏检、派单是否带锚点
- **正向**:打回率不升/略降,reviewer 质量稳定,锚点用上
- **负向(严重)**:打回率明显上升 → implementer 关思考降质,需评估复杂任务切 think
- **负向(小)**:个别规范漏执行 → 补进清单

### 指标 6:A+B 后续方案执行风险
- **查法**:若 #37/#38/#39 后续执行,执行时查是否又踩"删定义/改签名但调用方没同步"的坑
- **正向**:执行前有 grep 调用方 + 跑真实链路,无新 NameError
- **负向(严重)**:又出同类 NameError → 流程缺失,需落防再犯硬规范

## 巡检节奏
- **每天 18:05 触发 cron 自动巡检**(避开 15:35/16:00/17:50/20:35/22:00 盘后任务;周六休市可查数据完整性)
- 巡检结果落档:新增 `docs/inspection-YYYYMMDD.md` 或追加本文件「巡检记录」节
- 关键节点:**8/17 周一 18:05 是核心验证**(真实交易日盘中宽度指标是否恢复)

## 止损/迭代规则
- **严重(止损回滚)**:采集持续失败影响线上数据 / 白屏 / 数据错误 / 会话膨胀到百 MB → 立即回滚对应改动,查根因再上
- **小问题(迭代优化)**:打回率略升 / 规范漏执行 / 偶发超时 → 记入清单,下轮迭代优化
- 巡检发现的一切记录都**如实标注**,不选择性隐瞒(§5.1 诚实标注)

## 巡检记录

<!-- 每次巡检追加日期 + 6 项指标结果 + 判定 -->
### 2026-08-15(周六,首次巡检;batch 上线后 + 免费异源实施中)

| 指标 | 结果 | 证据 | 判定 |
|---|---|---|---|
| 1 线上 collect_health | **不再报 `_now`** | overview.json collect_health level=error,但只剩 2 项独立 error:`a_qvix_300/a_qvix_1000`(optbbs 源超时,独立故障待修),`_now` 错误消失 | ✅ 正向(#90 修复生效上线) |
| 2 #90 数据补齐 | **8/14 值已补** | daily_metric `us10y`=4.68(8/14)、`a_fund_margin`=13549.98(8/14) | ✅ 正向 |
| 3 8/17 周一盘中写入 | **待验证** | 今天 8/15 周六休市,无盘中;8/17 周一盘后必查(核心验证点) | ⏳ 待 8/17 |
| 4 主控 jsonl 大小 | 28M(本会话 a84dd439) | ls 查 jsonl,当前主会话 28M,未到百MB级 | ✅ 正向(未膨胀;但已近 clear/compact 时机) |
| 5 执行质量(打回率) | **0 次 FAIL** | batch 首轮 PASS带7P2、P2复查 PASS带1新P2(README 26×已修),无 FAIL;免费异源 reviewer 运行中 | ✅ 正向(打回率不升) |
| 6 A+B 后续方案风险 | **无新 NameError** | #37/#39 未执行(pending),#38 已完成调研;`_now` 修复已验证生效(指标1) | ✅ 正向(暂未暴露) |

**总判定**:✅ 正向发展(4 项确认),1 项待 8/17 核心验证(指标 3),无严重问题无需止损回滚。
**小问题/迭代**:
- qvix(optbbs)源宕机是**独立遗留故障**(非 #90 回归),免费异源实施正在处理(optbbs→sse→rv 3重),下交易日观察是否兜住。
- 主控 jsonl 28M 已近 clear/compact 时机(P0-1 落档的主动瘦身待执行)。
- reviewer 的 README 26× 倍数错误已主控 A 级修正(§0.1 例外,紧急上线链)。

### 2026-08-15 补充(18:40,核对 launchd 退出码发现的巡检盲区)

**发现**:巡检指标 1 只查 `overview.json` 的 collect_health,漏了 launchd 层退出码。核对 `launchctl list | grep trade` 发现 2 个任务 8/15 退出码 1:
| 任务 | 时点 | 失败根因 | 影响 | 现状 |
|---|---|---|---|---|
| gold-night | 8/15 02:40 | `runner.py` `_now` 未定义(be3da2c94 删定义,当时修复 5e70d8bc7 尚未提交) | gold_night 采集失败,发告警(已 email/feishu) | 17:20 commit 已修复;gold 8/14=943.16 已在 DB(akshare 兜) |
| us-stock-morning | 8/15 05:00 | 同上(us10y 链路) | us10y 8/14 收盘没采到,整体 exit 1 跳过 deploy | 17:20 commit 已修复;us10y 8/14=4.68 已在 DB |

**判定**:历史故障(#90 同源),17:20 commit + 17:50 update_all 后已修复,非当前持续问题。数据已兜底,用户视角无缺。
**巡检盲区修正(记入下轮)**:
- 指标 1 扩查:除 `overview.json collect_health` 外,必查 `launchctl list | grep trade` 退出码非 0 的任务(尤其 02:40/05:00 凌晨任务,collect_health 不覆盖)。
- 教训:8/15 凌晨两任务失败当时没被巡检抓到的根因 = 巡检只看 collect_health 不看 launchd exit code + 这两任务不在「巡检对象 4 项改动」直接相关链。下轮巡检两个指标并行。
