# CLAUDE.md 瘦身方案 A1 + §5.4 状态独立（2026-08-20 用户拍板方向: A1 + §5.4独立）

> 目的:把 CLAUDE.md(39KB/392行)中"过时重复的元信息"和"状态型基准定义"清掉。
> 方向:用户选 **A1(瘦§18元信息) + 方案2(§5.4基准锚点独立)**。
> 状态:本文件=改造方案草案,待用户高峰后过目 → 派 implementer 应用。主文件 CLAUDE.md 本轮不动。

## 为什么这么瘦(核心判断)
- CLAUDE.md 三大块 §18(10.8K)/§23(12.2K)/§5(7K) 占 77%。§23 是活跃规范不动;§5 以 §5.4(5K)为大头;§18 以 7 条超长行(4.6K)为大头。
- **§18 超长行=重灾区教训核心根因,不动(保核心)**;只清§18 的"元信息冗余"(移动去向/已提炼引用清单,约995B)。
- **§5.4 基准定义是"状态型内容"**,权威已由 memory `test-baseline-v112-anchor` 承载(每会话注入、含 v1.1.2 版本链对照/四档口径/复现数字,比 §5.4 正文更全)。§5.4 里那份是重复副本 → 删掉不丢核心(§5.3 核心保留=memory 有权威版),且消除"双份状态不同步"隐患。
- **§5.4 行为规范(②③④⑤⑥)必须留根**——是派单/回测纪律,非状态。

## 改动清单(待 implementer 应用)
### 1. §18 删元信息(约 -995B,零丢核心)
- L98「移动去向」整行删(§11/15/21/22/23.x→role skill 的转移说明,属历史归档元信息,正式章节已权威)。
- L163+L164「已提炼防重犯条款引用清单」两行删。**但注意**:此清单是"防重犯→正式条款"导航,删前确认其指向的正式章节(§11/14/15/21/22/23.1/23.2/23.3 §0.1)在根文件别处仍可见——是的(§0.1/§21/§22/§23.x 都在根),属重复导航,可删。

### 2. §5(§5.4) 状态型拆独立(约 -3.5K)
- **§5.4 标题「测试基准锚点铁律」改为**指向:注明"当前基准版本定义(状态型)=见 memory `test-baseline-v112-anchor`,每会话注入;本节约行为纪律"。
- **L65「核心一句话」整段(当前基准版本内容)删**——权威在 memory;根只留铁律精神"任何测试偏离当前基准=违规"。
- **L66-68(①基准定义 / v1.1.1增量 / v1.1.0增量依据)整段删**——全是"状态型"基准版本,memory 有完整 v1.1.2 版。
- **L69(②测试前提=基准)/L70(③派单钉基准)/L71(④偏离)/L72(⑤与§5.1)/L73(⑥版本升级原则)保留**——行为纪律留根。
- **③L70 的引用锚点「§5.4①」改为「memory test-baseline-v112-anchor」**(因为①定义的组合挪走了,引用要指向 memory 权威)。

### 3. 核心保留清单(§5.3 最小核心,删后必须都在)
| 核心内容 | 删后保留位置 |
|---|---|
| 测试基准"任何偏离=违规"纪律 | §5.4 L69/② 留根 |
| 派单钉基准的引用锚点 | §5.4 L70/③ 改为 memory 锚点 |
| 当前基准版本定义(状态) | memory test-baseline-v112-anchor(MEMORY.md 索引,每会话注入) |
| 版本链对照(v1.0-v1.1.2) | memory test-baseline-v112-anchor「版本链对照」段 |
| 版本升级原则(动核心组合必发版本) | §5.4 L73/⑥ 留根 |
| 防重犯条款正文 | 各 role skill + §23.x + memory(§18 只删元信息,没删条款) |
| §18 重灾区教训根因 | §18 超长行保留不动 |

### 4. 可逆性
- 纯文案删除 + 状态型挪到 memory(不动各 role skill / 生产代码 / 数据)。
- git 可整体 revert(改动只碰 CLAUDE.md + 可选新建文档,无跨文件逻辑依赖)。
- memory `test-baseline-v112-anchor` 已存在且完整,不新建不修改,零风险。

## 预期收益
- §18: 10.8K → ~9.8K(-1K)
- §5(含§5.4): 7K → ~3.5K(-3.5K)
- CLAUDE.md 总: 39K → ~34.5K(-4.5K,约-12%)
- 顺带消除:§5.4 正文与 memory 基准"双份状态不同步"隐患(§5.4 停在更简版,memory 是权威版)。

## 待办
- [ ] 用户高峰后过目本方案 → 派 implementer 应用
- [ ] implementer 应用后 self-检查核心保留清单(§5.3)
- [ ] reviewer 回归:CLAUDE.md 改后各章节引用(memory/§5.4③/role skill)无断链
- [ ] git 提交(用户让提交才提交;当前用户未让 push author 明文)

## 复现
- 本方案为文案重构,无数据/脚本生成;核心保留清单手工核对。改动落位=git diff 可追踪(revert 即恢复)。
