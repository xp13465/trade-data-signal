# PROJECT-SPECIFIC.md 项目专项模板

> 把本协作标准移植到新项目时,复制本模板到项目根(命名 `PROJECT-SPECIFIC.md`),替换全部 `<尖括号>` 占位符为新项目实际值,**删除与本项目无关的章节条目**,保留章节骨架即可。**状态型内容(时点表/现网端点/当前配置值)只写本文件,绝不写进 skill**(skill 会过时,项目专项是活文档)。
>
> 状态型内容维护纪律:每次依赖现网状态的改动,同步更新本文件对应值;skill 只写「去 PROJECT-SPECIFIC.md 查」,不写死现网值。

---

## 0. 项目概览

- 一句话定位: `<一句话说明这个项目做什么>`
- 主技术栈: `<语言/框架/部署方式>`
- 代码仓库: `<仓库地址>`(公开仓与内网仓分工按需说明)
- 纯开发机 vs 生产机分工: `<本机是否跑定时任务 / 生产机在哪里>`

## 1. 文件结构

- 共享核心: 根 `<CLAUDE.md / AGENTS.md>`
- 主控专属规范: `<docs/main-governance.md>`
- 角色执行层: 4 个 agent 定义 + 4 个 role skill(目录按本标准的实际布局填写)
- 项目专项: 本文件
- 数据产物目录(部署对象): `<数据产物目录路径>`
- 本地开发数据(不应该进 git): `<路径列表,见 .gitignore>`

## 2. 推送

- 主分支: `main`;开发分支: `feat/*`;merge/推 main 统一入口脚本:`<scripts/..merge脚本路径>`
- 提交信息末尾加:`Co-Authored-By: Claude <noreply@anthropic.com>`
- 不该进 git 的本地数据/密钥:`<列表>`
- 线上验证优先级:`<主域名> 优先 / <备站1> / <备站2>`(任一域名到新版即算 OK)

## 3. 数据产物上线

- 部署脚本:`<部署脚本路径>`(CDN/对象存储推送架构按本项目实际情况写)
- 产物推送机制说明:`<部署脚本自动做什么/manual 步骤>`
- 产物上线分支约束:`<是否只能在 main 分支部署等>`

## 4. 前端铁律(版本串方案)

- 前端源码目录:`<前端源码目录>`
- 版本串机制:`<发布序号 vs 内容哈希,当前用哪种>`
- min/gzip 重建脚本:`<构建产物脚本路径>`
- 版本串 bump 脚本:`<bump版本串脚本路径>`
- Service Worker / 缓存层: `<SW 文件路径 + 缓存版本常量位置>`
- 防撕裂四件(改前端必做): ①版本串每次部署强制换新串 ②改源码必同 commit bump + 重建 min ③SW 壳芯配套 + 失败回退 ④`<部署后校验脚本>` 校验内容哈希==引用版本串
- 上线必须完整版: 数据就绪清单 + 覆盖范围/展示范围核对 PASS + 无降级展示项

## 4.2 测试基准锚点(当前基准版本定义,权威)

- **当前基准版本**: `<版本号 e.g. v1.1.7>`
- **基准定义**: `<默认组合/关键参数/口径,精确到每次回测都能复现>`
- **复现数字**: `<基线回测的权威数字,供回测前头对头对齐>`
- **版本链**: `<已发过的全部版本 tag 清单>`
- **升级原则**: 动到核心推荐/过滤类核心实用功能默认组合/算法 → 必须发中间版本 + 同步本文件 + 前端默认值 + 公示 + README + 全部键集登记点(见 §8.1)
- 改算法/基准后: 更新本文件 + 前端默认值 + 公示 + README + 全部键集登记点机检 PASS

## 5. 定时任务时点(盘后安全窗口最低要求)

> 状态型,维护到本文件。**默认最低要求(任何时候都不违反)**: `<三防时点清单,如: 15:35/16:00/17:50/20:35/22:00 不推 main 不写关键数据库>`;交易日盘中不跑全量部署;安全窗口 `<23:00 后>` 无调度风险。
> 本项目精确任务清单/时点:`<任务名 | 时点 | 做什么 | 归属机(本机/云上)>`

## 6. 生产稳定性

- 定时任务托管:`<定时任务承载机制(本机是否跑/生产机在哪/用什么方式管理,git 更新是否生效)>`
- 歇机判定:`<判数据/任务停摆必须 ssh 生产机查,不查本机>`
- 时点禁区摘要:见 §5 清单(15:35/16:00/17:50/20:35/22:00 等,维护时同步更新)

## 7. 回归

- smoke 清单: `<docs/smoke-checklist.md>`(P0/P1 主功能点,每次上线前跑)
- 改动分级 A/B/C + review 深度:`A 级=纯显示主控直接改 / B 级=逻辑派实施+reviewer / C 级=数据后端+reviewer+数据完整性校验`

## 8. 一致性

- 数据一致性铁律(根共享核心 §22): 用户在 N 个展示位看到的数据必须统一;策略只决定何时更新/排序,一旦更新必须 N 文件+缓存同步
- 关键展示位清单:`<展示位1: 数据产物来源> / <展示位2: …>`
- 缓存层:`<本地缓存 / 边缘缓存 / 对象存储缓存 各自位置与清理方式>`
- 键集登记点清单(同一事实多处副本): `<配置1位置 / 配置2位置 / 前端默认 / 公示文件 / 通知链路>`;改动任一必须全量 grep 同步 + 机检一致(挂部署链 FAIL 阻断)

## 9. 三条用户铁律(详见根共享核心 §23)

1. README 维护: 功能完成必补 README(参考与致敬段+主体段)
2. 修 bug 三铁律: 修完整+自测完成+排查同类
3. 做方案举一反三: 主动覆盖同模式/同数据源/同组件还被谁用

## 10. 速查(按任务类型怎么直接做对)

- 上线一个新功能: `<步骤>`
- 修一个 bug: `<步骤>`(修 bug 三铁律)
- 跑一次回测: `<步骤>`(钉当前基准,非基准口径显式声明)
- 各角色详细 SDS 见格式文档 `<docs/agent-quickstart.md>`(按任务类型分成,读对应小节)

## 11. 教训速查

本标准的通用教训索引:`docs/lessons-index.md` + 归档文件 `<docs/archive/CLAUDE-errors-<月份>.md>`(全文反追);命中场景读索引 → grep 锚点 → 归档原文

## 12. memory 机制(本项目的持久记忆约定)

- 入口指针文件:`<memory 索引位置>`(项目根或全局,开工先读)
- 记忆分类:`<项目入口指针 / 教训速查 / 方案速查 / 状态型锚点>`;状态型锚点(测试基准/现网配置)以项目专项 + memory 双登记

---

## 状态型内容维护记录(改一次记一行,防漂移)

| 日期 | 改动内容 | 关联 commit |
|---|---|---|
| `<YYYY-MM-DD>` | `<改了哪个状态型值/为什么>` | `<hash>` |