fastapi>=0.110
uvicorn[standard]>=0.29
akshare==1.18.64
mootdx==0.11.7
stockstats==0.6.8
pyyaml>=6.0
python-dateutil>=2.9
# MiniRacer (V8 引擎, akshare 加密 JS 接口依赖)
# 用 bpcreech 的 mini-racer (提供 py_mini_racer 模块, 含 macOS arm64 dylib)
# 不要装 sqreen 的 py-mini-racer==0.6.0: 它只带 muslc.so (Linux), 会覆盖
# __init__.py 导致 ctypes 找不到 mr_eval_context 符号, akshare 指数全挂.
# mootdx 依赖 py-mini-racer 但运行时 import py_mini_racer 由 mini-racer 满足.
mini-racer==0.14.1
